A static HTML/CSS/JS portfolio site. Designed from the ground up for a clean **Next.js migration** — every section maps to a future React component, every CSS file maps to a future CSS Module, and the JS is structured as self-contained blocks ready to become `useEffect` hooks.
